{
  "name": "govuk-pins",
  "version": "1.0.0",
  "description": "PINS Library Service subtheme of the GOV.UK Drupal theme",
  "author": "PINS",
  "license": "ISC",
  "engines": {
    "node": ">=22.0.0"
  },
  "private": true,
  "scripts": {
    "build": "node --no-deprecation node_modules/.bin/gulp",
    "watch": "node --no-deprecation node_modules/.bin/gulp watch"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-cli": "^3.0.0",
    "gulp-sass": "^6.0.0",
    "gulp-sass-variables": "^1.2.0",
    "gulp-sourcemaps": "^3.0.0",
    "sass": "^1.97.3"
  }
}
